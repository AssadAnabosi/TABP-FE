# Base URL prepended to every /api call. Leave empty in dev (the Vite proxy forwards /api to the backend).
VITE_API_BASE_URL=
# Dev-server only: where the Vite proxy forwards /api (default http://localhost:8080).
# VITE_API_PROXY_TARGET=http://localhost:8080
